export const metadata = {
  title: "Admin Dashboard | Paycort",
  description: "Paycort Admin Dashboard - Manage Waitlist",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}